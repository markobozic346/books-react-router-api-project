import React from 'react'
import { useParams } from 'react-router'

const Book = () => {
    const { id } = useParams();
    return (
        <div>
            <h1>mjau</h1>
        </div>
    )
}

export default Book
